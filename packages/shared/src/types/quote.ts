import { z } from 'zod';\nimport { QuoteStatus, ApprovalType, ApprovalStatus } from '../constants';\n\nexport const QuoteSchema = z.object({\n  id: z.string(),\n  dbr: z.string(),\n  customerId: z.string(),\n  executiveId: z.string(),\n  distributor: z.string(),\n  country: z.string(),\n  currency: z.string().default('BRL'),\n  status: z.nativeEnum(QuoteStatus),\n  contractMonths: z.number().int().positive(),\n  totalMargin: z.string(), // Decimal as string\n  arr: z.string(),\n  mrr: z.string(),\n  tcv: z.string(),\n  totalBRL: z.string(),\n  totalEUR: z.string(),\n  exchangeRate: z.string(),\n  createdAt: z.date(),\n  updatedAt: z.date(),\n});\n\nexport type Quote = z.infer<typeof QuoteSchema>;\n\nexport const CreateQuoteSchema = z.object({\n  dbr: z.string(),\n  customerId: z.string(),\n  executiveId: z.string(),\n  distributor: z.string(),\n  country: z.string(),\n  currency: z.string().default('BRL'),\n  contractMonths: z.number().int().positive().default(60),\n});\n\nexport type CreateQuote = z.infer<typeof CreateQuoteSchema>;\n\nexport const QuoteLineSchema = z.object({\n  id: z.string(),\n  quoteProductId: z.string(),\n  skuId: z.string(),\n  quantity: z.string(),\n  discount: z.string(),\n  listPrice: z.string(),\n  offeredPrice: z.string(),\n  grossPrice: z.string(),\n  netPrice: z.string(),\n  margin: z.string(),\n  costPrice: z.string(),\n  minimumGross: z.string(),\n  notes: z.string().optional(),\n  violatesMinimum: z.boolean(),\n  needsException: z.boolean(),\n  createdAt: z.date(),\n  updatedAt: z.date(),\n});\n\nexport type QuoteLine = z.infer<typeof QuoteLineSchema>;\n\nexport const CreateQuoteLineSchema = z.object({\n  quoteProductId: z.string(),\n  skuId: z.string(),\n  quantity: z.string(),\n  discount: z.string().default('0'),\n  notes: z.string().optional(),\n});\n\nexport type CreateQuoteLine = z.infer<typeof CreateQuoteLineSchema>;\n\nexport const ApprovalSchema = z.object({\n  id: z.string(),\n  quoteId: z.string(),\n  approverId: z.string(),\n  type: z.nativeEnum(ApprovalType),\n  status: z.nativeEnum(ApprovalStatus),\n  reason: z.string(),\n  comment: z.string().optional(),\n  createdAt: z.date(),\n  updatedAt: z.date(),\n  decidedAt: z.date().optional(),\n});\n\nexport type Approval = z.infer<typeof ApprovalSchema>;\n