import { z } from 'zod';\n\nexport const SKUSchema = z.object({\n  id: z.string(),\n  productId: z.string(),\n  code: z.string(),\n  description: z.string(),\n  commercialLine: z.string(),\n  profitCenter: z.string(),\n  materialType: z.string(),\n  pbu: z.string(),\n  dynamics365Code: z.string().optional(),\n  unit: z.string(),\n  listPrice: z.string(),\n  minimumGross: z.string(),\n  costPrice: z.string(),\n  active: z.boolean(),\n  createdAt: z.date(),\n  updatedAt: z.date(),\n});\n\nexport type SKU = z.infer<typeof SKUSchema>;\n\nexport const PricingCalculationSchema = z.object({\n  listPrice: z.string(),\n  discount: z.string(), // percentage\n  minimumGross: z.string(),\n  costPrice: z.string(),\n  quantity: z.string(),\n});\n\nexport type PricingCalculation = z.infer<typeof PricingCalculationSchema>;\n\nexport const PricingResultSchema = z.object({\n  offeredPrice: z.string(),\n  grossPrice: z.string(),\n  netPrice: z.string(),\n  margin: z.string(), // percentage\n  violatesMinimum: z.boolean(),\n});\n\nexport type PricingResult = z.infer<typeof PricingResultSchema>;\n\nexport const EngagementSchema = z.object({\n  id: z.string(),\n  quoteId: z.string(),\n  value: z.string(),\n  installmentValue: z.string(),\n  installments: z.number().int().positive(),\n  recurringValue: z.string(),\n  monthlyRecurring: z.string(),\n  contractMonths: z.number().int().positive(),\n  createdAt: z.date(),\n  updatedAt: z.date(),\n});\n\nexport type Engagement = z.infer<typeof EngagementSchema>;\n