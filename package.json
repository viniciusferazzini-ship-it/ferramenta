{
  "name": "ferramenta-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "A full-stack pricing tool application",
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "lint": "pnpm -r run lint"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
